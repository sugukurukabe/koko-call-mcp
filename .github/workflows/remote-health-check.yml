name: Remote Health Check

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  remote:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run remote:health
      # remote:mcp は OAuth 保護下のため CI では 401 が正常動作。
      # ローカルでの手動確認用に残す。
      # - run: npm run remote:mcp
