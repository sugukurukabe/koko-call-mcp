# v0.9.0 Release Evidence

## 日本語

このファイルは JP Bids MCP v0.9.0 の公開時に、同一SHAから生成された証拠を残すための記録です。公開前は `pending` を残し、公開操作後に実値へ更新する。

| 項目 | 値 |
|---|---|
| Version | 0.9.0 |
| Commit SHA | pending |
| Container image digest | pending |
| npm package | pending |
| GitHub Release | pending |
| MCP Registry submission | pending |
| Server card | `public/.well-known/mcp/server-card.json` |
| SBOM | pending |
| Checksums | pending |

検証順序:

1. `npm run release:gate`
2. exact SHA のcontainer image build / deploy
3. remote OAuth / MCP / App smoke（Investor Radar 含む）
4. npm trusted publish（`v0.9.0` タグ push → GitHub Actions）
5. GitHub Release with SBOM, checksum, image digest
6. MCP Registry publish
7. Zenn publication and post-publication remote smoke

## English

This file records evidence that JP Bids MCP v0.9.0 was released from a single commit SHA. Leave `pending` before publication and replace values after each release operation.

## Bahasa Indonesia

File ini mencatat bukti bahwa JP Bids MCP v0.9.0 dirilis dari satu commit SHA. Biarkan `pending` sebelum publikasi dan ganti dengan nilai aktual setelah setiap langkah rilis.
